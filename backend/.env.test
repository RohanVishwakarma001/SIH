# Overrides applied only when NODE_ENV=test (see src/config/env.ts).
# Keeps the test suite fast, deterministic, offline, and free of API cost —
# independent of whatever AI_PROVIDER is configured in .env for dev/production.
AI_PROVIDER=mock
